import { styled } from "@mui/system";
import { Box } from "@mui/material";

export const DefaultPageLayoutContainer = styled(Box)({
  height: "100vh",
  display: "flex",
  flexDirection: "column",
  justifyContent: "space-between",
});
