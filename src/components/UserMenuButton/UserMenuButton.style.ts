import { styled } from "@mui/system";
import Button from "@mui/material/Button";

export const UserMenuButtonContainer = styled(Button)({
  borderBottomLeftRadius: "0px",
  borderBottomRightRadius: "0px",
  width: "120px",
});
