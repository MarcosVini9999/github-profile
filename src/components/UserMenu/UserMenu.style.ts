import { styled } from "@mui/system";

export const UserMenuContainer = styled("div")({
  width: "100%",
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  marginBlock: "20px",
});

export const UserMenuController = styled("div")({
  display: "flex",
  flexDirection: "row",
  justifyContent: "space-evenly",
  flexWrap: "wrap",
});
